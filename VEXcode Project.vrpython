{"mode":"Text","textContent":"# ------------------------------------------\n# \n# \tProject:      VEXcode Project\n#\tAuthor:       VEX\n#\tCreated:\n#\tDescription:  VEXcode VR Python Project\n# \n# ------------------------------------------\n\n# Library imports\nfrom vexcode import *\ndrivetrain.set_drive_velocity(100,PERCENT)\n\n\ndef set_up_pos():\n    drivetrain.turn_for(LEFT, 90, DEGREES)\n    drivetrain.drive_for(FORWARD, 800, MM)\n    drivetrain.turn_for(RIGHT, 90, DEGREES)\n\ndef clean_up():\n    turning_count = 0\n    while True:\n        #Face up when at bottom\n        if location.position(Y, MM) == -800:\n            drivetrain.set_heading(0, DEGREES)\n        #Face down when at top\n        if location.position(Y, MM) == 800:\n            drivetrain.set_heading(180, DEGREES)\n        #\n        drivetrain.drive_for(FORWARD, 200, MM)\n        # check is the car is on the right or left: 0 for on the left\n        if turning_count == 0:\n            drivetrain.turn_for(RIGHT, 90, DEGREES)\n        else:\n            drivetrain.turn_for(LEFT, 90, DEGREES)\n        \n        dis = distance.get_distance(MM)\n        if dis >= 1500:\n            if turning_count == 0:\n                drivetrain.turn_for(LEFT, 90, DEGREES)\n            else:\n                drivetrain.turn_for(RIGHT, 90, DEGREES)\n            continue\n            \n        else:\n            drivetrain.drive_for(FORWARD, 1600, MM)\n            if turning_count == 0:\n                drivetrain.turn_for(LEFT, 90, DEGREES)\n                turning_count += 1\n            else:\n                drivetrain.turn_for(RIGHT, 90, DEGREES)\n                turning_count = 0\n\n# Add project code in \"main\"\ndef main():\n    set_up_pos()\n    clean_up()\n# VR threads — Do not delete\nvr_thread(main())\n","textLanguage":"python","rconfig":[],"slot":0,"platform":"PG","sdkVersion":"","appVersion":"","fileFormat":"1.0.0","icon":"","playground":"CoralReefRescue"}